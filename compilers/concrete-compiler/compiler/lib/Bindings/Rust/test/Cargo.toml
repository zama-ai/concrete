[package]
name = "concrete-test"
version = "0.0.0"
edition = "2021"

[dependencies]
concrete-macro = { version = "2.10.1-rc1", path = "../concrete-macro" }
concrete = {  version = "2.10.1-rc1", path = "../concrete"  }
